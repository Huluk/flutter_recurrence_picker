/// Non-Flutter utilities from the recurrence_picker package.
///
/// Use this import in files that don't depend on Flutter
/// (e.g. entity/model files) to avoid pulling in widget code.
library;

export 'src/rrule_utils.dart';
