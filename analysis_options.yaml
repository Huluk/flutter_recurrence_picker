include:
  - package:flutter_lints/flutter.yaml
linter:
  # A list of all available lints is published at https://dart.dev/lints.
  rules:
    # Imports.
    directives_ordering: true
    avoid_relative_lib_imports: true # test/ should not relative import lib/.
    prefer_relative_imports: true # within lib/, use relative imports.
    # sort_pub_dependencies: true # sort pubspec.yaml alphabetically.

    # Futures.
    discarded_futures: true
    unawaited_futures: true
    unnecessary_await_in_return: true

    # Simplifications: Bool edition.
    avoid_bool_literals_in_conditional_expressions: true
    avoid_positional_boolean_parameters: true
    literal_only_boolean_expressions: true
    no_literal_bool_comparisons: true
    use_if_null_to_convert_nulls_to_bools: true

    # Simplifications: Everything else.
    unnecessary_breaks: true
    unnecessary_lambdas: true
    use_truncating_division: true
    unnecessary_parenthesis: true
    use_named_constants: true
    unnecessary_statements: true

    # Strings.
    leading_newlines_in_multiline_strings: true
    missing_whitespace_between_adjacent_strings: true
    no_adjacent_strings_in_list: true
    unnecessary_raw_strings: true

    # Specific Functions.
    # avoid_print: false
    only_throw_errors: true

    # Misc errors.
    no_self_assignments: true

    # Classes and arguments.
    parameter_assignments: true
    prefer_const_constructors: true
    prefer_asserts_in_initializer_lists: true
    prefer_const_literals_to_create_immutables: true
    prefer_constructors_over_static_methods: true
    sort_unnamed_constructors_first: true
    use_enums: true

    # Type annotations.
    prefer_final_in_for_each: true
    prefer_final_locals: true
    prefer_void_to_null: true

    # Formatting.
    # prefer_single_quotes: true
    lines_longer_than_80_chars: true

# Additional information about this file can be found at
# https://dart.dev/guides/language/analysis-options

formatter:
  trailing_commas: preserve
